name: Secrets

on: [push]

jobs:

  Explore-GitHub-Actions:
    name: dev
    runs-on: ubuntu-18.04
    env:
      ENVIRONMENT: '${{ secrets.DEPLOY_ENV }}'
      DOMAIN: ${{ secrets[format('{0}_DOMAIN', env.ENVIRONMENT)] }}
    steps:
      - run: echo $DOMAIN
